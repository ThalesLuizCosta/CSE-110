tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $")) # lembrando sempre que essa linha nunca pode ficar longe do print final, pois dará chance para uma procura por uma variável ou função que não existe.
if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))

if tip < 0:
    print("Sorry, the tip cannot be nagative")
    tip = float(input("What is the tip amount? $"))
    
    # como mostrado nesse caso, eu posso mostrar quantas vezes for, o resultado será um único só.

print(f"You have tipped an amount of ${tip:.2f}")

# Veja que para um resultado negativo ele usará o $-5.50 para dizer que vc teve uma gorjeta negativa, mas isso não faz sentido, então usamos o "if tip < 0:"